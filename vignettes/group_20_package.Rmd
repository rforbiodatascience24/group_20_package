---
title: "dogma20"
output: rmarkdown::pdf_document
vignette: >
  %\VignetteIndexEntry{dogma20}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(dogma20)
```

# Description

## Global description

## create_random_seq

## replace_T_with_U

## split_codons

This function takes as input:

-   A string representing a nucleotide sequence - e.g. DNA or RNA.

-   An integer specifying the start position in the sequence - default is 1,
    meaning the function will start splitting from first position.

The function splits the sequence into strings of three characters (nucleotides) starting from the start position. It returns a vector of the strings representing the codons.

## convert_seq_to_prot

## plotting_amino_acid_distribution

# Use cases

# Discussion
